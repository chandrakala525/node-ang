var express = require('express');
var app = express();


// app.use(express.static('public'));

// app.use('/assets', express.static(node-ang-ex + '/assets'));

app.use(express.static(ang-node-ex, 'assets'));

app.listen(3000);